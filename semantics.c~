#include "semantics.h"
/*
//float to string
char*f(float n){
char *b = (char*)malloc(64);
int ret = snprintf(b, sizeof b, "%f", n);
cout<<"Converted Value: "<<b<<endl;
return b;
}

//createExpr(1,$1,1,NULL);} 
exptype*createExpr(int i, char* c, int id1, char*n){
exptype* t = (exptype*)malloc(sizeof(struct exptype));
if(id1==1)
{

}
else{
t-> type=i;
t->val = c;
t->id=id1;
t->name = n;
}

return t;
}

*/
